################################################################################
# Auto-generated from pr-test.yml.j2 — do not edit manually.
################################################################################
name: PR Test

on:
  pull_request:
    types: [labeled, synchronize]
  workflow_dispatch:

concurrency:
  group: pr-test-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      head_sha: ${{ github.event.pull_request.head.sha }}

      has_qwen3: ${{ steps.detect.outputs.has_qwen3 }}
      has_qwen3_moe: ${{ steps.detect.outputs.has_qwen3_moe }}

    steps:
      - name: Detect changed files
        id: detect
        uses: actions/github-script@v7
        with:
          script: |
            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              per_page: 100
            });
            const filenames = files.map(f => f.filename);

            const has_qwen3 = filenames.some(f =>
              ["qwen3/", "tests/qwen3/"].some(d => f.startsWith(d)) &&
              !["qwen3_moe/", "tests/qwen3_moe/"].some(d => f.startsWith(d))
            );
            core.setOutput('has_qwen3', has_qwen3);

            const has_qwen3_moe = filenames.some(f =>
              ["qwen3_moe/", "tests/qwen3_moe/"].some(d => f.startsWith(d))
            );
            core.setOutput('has_qwen3_moe', has_qwen3_moe);


      - name: Add labels to PR
        if: github.event.action == 'synchronize'
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const ciLabels = [];

            if ('${{ steps.detect.outputs.has_qwen3 }}' === 'true')
              ciLabels.push('run-ci-qwen3');

            if ('${{ steps.detect.outputs.has_qwen3_moe }}' === 'true')
              ciLabels.push('run-ci-qwen3-moe');

            if (ciLabels.length > 0) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                labels: ciLabels
              });
            }


  knowledge-qwen3-cpu:
    name: "CPU / knowledge-qwen3 (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3 == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-0.6B", "Qwen3-1.7B", "Qwen3-4B", "Qwen3-8B", "Qwen3-14B", "Qwen3-32B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3/test_knowledge.py -m slow -v -s --model ${{ matrix.model }} --device cpu


  knowledge-qwen3-cuda:
    name: "NVIDIA / knowledge-qwen3 (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3 == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-0.6B", "Qwen3-1.7B", "Qwen3-4B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3/test_knowledge.py -m slow -v -s --model ${{ matrix.model }} --device cuda


  accuracy-qwen3-cpu:
    name: "CPU / accuracy-qwen3 (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3 == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-0.6B", "Qwen3-1.7B", "Qwen3-4B", "Qwen3-8B", "Qwen3-14B", "Qwen3-32B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3/test_accuracy.py -m slow -v -s --model ${{ matrix.model }} --device cpu


  accuracy-qwen3-cuda:
    name: "NVIDIA / accuracy-qwen3 (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3 == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-0.6B", "Qwen3-1.7B", "Qwen3-4B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3/test_accuracy.py -m slow -v -s --model ${{ matrix.model }} --device cuda


  knowledge-qwen3-moe-cpu:
    name: "CPU / knowledge-qwen3-moe (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3_moe == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3-moe')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-30B-A3B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3_moe/test_knowledge.py -m slow -v -s --model ${{ matrix.model }} --device cpu


  accuracy-qwen3-moe-cpu:
    name: "CPU / accuracy-qwen3-moe (${{ matrix.model }})"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_qwen3_moe == 'true'
      || (github.event.action == 'labeled' && github.event.label.name == 'run-ci-qwen3-moe')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: ["Qwen3-30B-A3B"]
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="/media/hdddisk/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download Qwen/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest tests/qwen3_moe/test_accuracy.py -m slow -v -s --model ${{ matrix.model }} --device cpu
