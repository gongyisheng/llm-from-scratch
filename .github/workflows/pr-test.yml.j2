<%- set model_families = {
    'qwen3_dense': {
        'label': 'run-ci-qwen3-dense',
        'src_dirs': ['qwen3/', 'tests/qwen3/'],
        'exclude_dirs': ['qwen3_moe/', 'tests/qwen3_moe/'],
    },
    'qwen3_moe': {
        'label': 'run-ci-qwen3-moe',
        'src_dirs': ['qwen3_moe/', 'tests/qwen3_moe/'],
        'exclude_dirs': [],
    },
    'gpt_oss': {
        'label': 'run-ci-gpt-oss',
        'src_dirs': ['gpt_oss/', 'tests/gpt_oss/'],
        'exclude_dirs': [],
    },
} -%>

<%- set test_jobs = {
    'knowledge-qwen3-dense-cpu': {
        'family': 'qwen3_dense',
        'device': 'cpu',
        'test_file': 'tests/qwen3/test_knowledge.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B', 'Qwen3-8B', 'Qwen3-14B', 'Qwen3-32B'],
        'hf_org': 'Qwen',
    },
    'knowledge-qwen3-dense-cuda': {
        'family': 'qwen3_dense',
        'device': 'cuda',
        'test_file': 'tests/qwen3/test_knowledge.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B'],
        'hf_org': 'Qwen',
    },
    'accuracy-qwen3-dense-cpu': {
        'family': 'qwen3_dense',
        'device': 'cpu',
        'test_file': 'tests/qwen3/test_accuracy.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B', 'Qwen3-8B', 'Qwen3-14B', 'Qwen3-32B'],
        'hf_org': 'Qwen',
    },
    'accuracy-qwen3-dense-cuda': {
        'family': 'qwen3_dense',
        'device': 'cuda',
        'test_file': 'tests/qwen3/test_accuracy.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B'],
        'hf_org': 'Qwen',
    },
    'knowledge-qwen3-moe-cpu': {
        'family': 'qwen3_moe',
        'device': 'cpu',
        'test_file': 'tests/qwen3_moe/test_knowledge.py',
        'models': ['Qwen3-30B-A3B'],
        'hf_org': 'Qwen',
    },
    'accuracy-qwen3-moe-cpu': {
        'family': 'qwen3_moe',
        'device': 'cpu',
        'test_file': 'tests/qwen3_moe/test_accuracy.py',
        'models': ['Qwen3-30B-A3B'],
        'hf_org': 'Qwen',
    },
    'knowledge-gpt-oss-cpu': {
        'family': 'gpt_oss',
        'device': 'cpu',
        'test_file': 'tests/gpt_oss/test_knowledge.py',
        'models': ['gpt-oss-20b'],
        'hf_org': 'openai',
    },
    'accuracy-gpt-oss-cpu': {
        'family': 'gpt_oss',
        'device': 'cpu',
        'test_file': 'tests/gpt_oss/test_accuracy.py',
        'models': ['gpt-oss-20b'],
        'hf_org': 'openai',
    },
} -%>

<%- set tp_jobs = {
    'knowledge-tp-qwen3-dense-cpu': {
        'family': 'parallel',
        'test_file': 'tests/qwen3/test_knowledge.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B'],
        'hf_org': 'Qwen',
        'nproc': 2,
    },
    'accuracy-tp-qwen3-dense-cpu': {
        'family': 'parallel',
        'test_file': 'tests/qwen3/test_accuracy.py',
        'models': ['Qwen3-0.6B', 'Qwen3-1.7B', 'Qwen3-4B'],
        'hf_org': 'Qwen',
        'nproc': 2,
    },
} -%>

name: PR Test

on:
  pull_request:
    types: [opened, labeled, synchronize]
    paths: ['**/*.py']
  workflow_dispatch:

concurrency:
  group: pr-test-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

permissions:
  pull-requests: write
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      head_sha: ${{ github.event.pull_request.head.sha }}
<% for name in model_families %>
      has_<< name >>: ${{ steps.detect.outputs.has_<< name >> }}
<%- endfor %>
      has_parallel: ${{ steps.detect.outputs.has_parallel }}

    steps:
      - name: Detect changed files
        id: detect
        uses: actions/github-script@v7
        with:
          script: |
            const files = await github.paginate(github.rest.pulls.listFiles, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.payload.pull_request.number,
              per_page: 100
            });
            const filenames = files.map(f => f.filename);
<% for name, config in model_families.items() %>
            const has_<< name >> = filenames.some(f =>
              << config.src_dirs | tojson >>.some(d => f.startsWith(d))<% if config.exclude_dirs %> &&
              !<< config.exclude_dirs | tojson >>.some(d => f.startsWith(d))<% endif %>
            );
            core.setOutput('has_<< name >>', has_<< name >>);
<% endfor %>
            const has_parallel = filenames.some(f =>
              ["parallel/"].some(d => f.startsWith(d))
            ) || has_qwen3_dense;
            core.setOutput('has_parallel', has_parallel);

      - name: Add labels to PR
        if: github.event.action == 'synchronize'
        continue-on-error: true
        uses: actions/github-script@v7
        with:
          script: |
            const ciLabels = [];
<% for name, config in model_families.items() %>
            if ('${{ steps.detect.outputs.has_<< name >> }}' === 'true')
              ciLabels.push('<< config.label >>');
<% endfor %>
            if ('${{ steps.detect.outputs.has_parallel }}' === 'true')
              ciLabels.push('run-ci-parallel');

            if (ciLabels.length > 0) {
              await github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.payload.pull_request.number,
                labels: ciLabels
              });
            }
<% for job_name, config in test_jobs.items() %>

  << job_name >>:
    name: "<% if config.device == 'cuda' %>NVIDIA<% else %>CPU<% endif %> / << job_name | replace('-' ~ config.device, '') >>"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_<< config.family >> == 'true'
      || contains(github.event.pull_request.labels.*.name, '<< model_families[config.family].label >>')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: << config.models | tojson >>
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="${{ runner.tool_cache }}/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download << config.hf_org >>/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: uv run pytest << config.test_file >> -m slow -v -s --model ${{ matrix.model }} --device << config.device >>
<% endfor %>
<% for job_name, config in tp_jobs.items() %>

  << job_name >>:
    name: "CPU / << job_name | replace('-cpu', '') >>"
    needs: prepare
    if: >-
      needs.prepare.outputs.has_<< config.family >> == 'true'
      || contains(github.event.pull_request.labels.*.name, 'run-ci-parallel')
    runs-on: [self-hosted]
    strategy:
      fail-fast: false
      matrix:
        model: << config.models | tojson >>
    env:
      HF_HOME: /media/hdddisk/.cache/huggingface
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.head_sha }}

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: uv sync --extra test

      - name: Download checkpoint
        run: |
          persistent="${{ runner.tool_cache }}/huggingface/${{ matrix.model }}"
          if [ ! -f "$persistent/config.json" ]; then
            uv run hf download << config.hf_org >>/${{ matrix.model }} --local-dir "$persistent"
          fi
          mkdir -p checkpoints
          ln -sfn "$persistent" checkpoints/${{ matrix.model }}

      - name: Run tests
        run: PYTHONPATH=. uv run torchrun --nproc_per_node=<< config.nproc >> -m pytest << config.test_file >> -m slow -v -s --model ${{ matrix.model }} --device cpu
<% endfor %>
